var expect  = require("chai").expect,
    check   = require("../lib/check.js");

describe("check.js", function () {
	describe("exports", function () {
		describe("isAlpha", function () {
			it("should be a function", function () {
				expect(check.isAlpha).to.be.a("function");
			});
		});
		describe("isAlphanumeric", function () {
			it("should be a function", function () {
				expect(check.isAlphanumeric).to.be.a("function");
			});
		});
		describe("isArguments", function () {
			it("should be a function", function () {
				expect(check.isArguments).to.be.a("function");
			});
		});
		describe("isArray", function () {
			it("should be a function", function () {
				expect(check.isArray).to.be.a("function");
			});
		});
		describe("isBoolean", function () {
			it("should be a function", function () {
				expect(check.isBoolean).to.be.a("function");
			});
		});
		describe("isDate", function () {
			it("should be a function", function () {
				expect(check.isDate).to.be.a("function");
			});
		});
		describe("isDecimal", function () {
			it("should be a function", function () {
				expect(check.isDecimal).to.be.a("function");
			});
		});
		describe("isDefined", function () {
			it("should be a function", function () {
				expect(check.isDefined).to.be.a("function");
			});
		});
		describe("isElement", function () {
			it("should be a function", function () {
				expect(check.isElement).to.be.a("function");
			});
		});
		describe("isEmail", function () {
			it("should be a function", function () {
				expect(check.isEmail).to.be.a("function");
			});
		});
		describe("isEmpty", function () {
			it("should be a function", function () {
				expect(check.isEmpty).to.be.a("function");
			});
		});
		describe("isEven", function () {
			it("should be a function", function () {
				expect(check.isEven).to.be.a("function");
			});
		});
		describe("isFinite", function () {
			it("should be a function", function () {
				expect(check.isFinite).to.be.a("function");
			});
		});
		describe("isFunction", function () {
			it("should be a function", function () {
				expect(check.isFunction).to.be.a("function");
			});
		});
		describe("isInt", function () {
			it("should be a function", function () {
				expect(check.isInt).to.be.a("function");
			});
		});
		describe("isIp", function () {
			it("should be a function", function () {
				expect(check.isIp).to.be.a("function");
			});
		});
		describe("isIpv4", function () {
			it("should be a function", function () {
				expect(check.isIpv4).to.be.a("function");
			});
		});
		describe("isIpv6", function () {
			it("should be a function", function () {
				expect(check.isIpv6).to.be.a("function");
			});
		});
		describe("isLowercase", function () {
			it("should be a function", function () {
				expect(check.isLowercase).to.be.a("function");
			});
		});
		describe("isNaN", function () {
			it("should be a function", function () {
				expect(check.isNaN).to.be.a("function");
			});
		});
		describe("isNegative", function () {
			it("should be a function", function () {
				expect(check.isNegative).to.be.a("function");
			});
		});
		describe("isNull", function () {
			it("should be a function", function () {
				expect(check.isNull).to.be.a("function");
			});
		});
		describe("isNumber", function () {
			it("should be a function", function () {
				expect(check.isNumber).to.be.a("function");
			});
		});
		describe("isObject", function () {
			it("should be a function", function () {
				expect(check.isObject).to.be.a("function");
			});
		});
		describe("isOdd", function () {
			it("should be a function", function () {
				expect(check.isOdd).to.be.a("function");
			});
		});
		describe("isPercentage", function () {
			it("should be a function", function () {
				expect(check.isPercentage).to.be.a("function");
			});
		});
		describe("isPort", function () {
			it("should be a function", function () {
				expect(check.isPort).to.be.a("function");
			});
		});
		describe("isPositive", function () {
			it("should be a function", function () {
				expect(check.isPositive).to.be.a("function");
			});
		});
		describe("isPrime", function () {
			it("should be a function", function () {
				expect(check.isPrime).to.be.a("function");
			});
		});
		describe("isRegExp", function () {
			it("should be a function", function () {
				expect(check.isRegExp).to.be.a("function");
			});
		});
		describe("isString", function () {
			it("should be a function", function () {
				expect(check.isString).to.be.a("function");
			});
		});
		describe("isUndefined", function () {
			it("should be a function", function () {
				expect(check.isUndefined).to.be.a("function");
			});
		});
		describe("isUnsignedInt", function () {
			it("should be a function", function () {
				expect(check.isUnsignedInt).to.be.a("function");
			});
		});
		describe("isUppercase", function () {
			it("should be a function", function () {
				expect(check.isUppercase).to.be.a("function");
			});
		});
		describe("isUrl", function () {
			it("should be a function", function () {
				expect(check.isUrl).to.be.a("function");
			});
		});
		describe("isWindow", function () {
			it("should be a function", function () {
				expect(check.isWindow).to.be.a("function");
			});
		});
    });
});